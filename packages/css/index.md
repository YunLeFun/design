# 样式示例

`packages/css` 收录 pulse、animation 等样式示例。旧 aurora 文件保留为历史示例，文档默认入口不再加载。这个目录没有 `package.json`，不是可安装的独立 npm 包。

共享主题统一维护在 `@yunlefun/ui`。成熟的通用效果可以在验证后收敛到该包的可选样式子路径，实验效果保留为示例。更多说明见[子包职责](/guide/packages)。
