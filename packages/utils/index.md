# @yunlefun/ui-utils

与 Vue 无关的 DOM 工具源码，目前主要提供 `previewElement` 元素放大预览。

这些工具需要浏览器 DOM 环境，不能在服务端直接调用。当前包尚未建立完整的构建、类型与导出入口，文档示例直接引用源码；它不是业务应用的必装依赖。

需要正式的弹窗交互和焦点管理时，优先使用 `YlfDialog`。完整分工见[子包职责](/guide/packages)。
